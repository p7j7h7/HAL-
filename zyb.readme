万年历作业
程序起始以  yyyy-mm-dd-hh：mm：ss.ms形式从0开始计时
定时器使用的TIM10  ARR和PSC均为999，HCLK设置为100MHZ   平均0.01秒进入一次中断，每次进入中断ms增加1，满100进位到秒。
串口使用的USART2    利用串口中断检测是否有串口输入数据，如有进入中断，串口输入为固定22位输入，0~3对应年，5~6对应月，8~9对应日，11~12对应时，14~15对应分，17~18对应秒，20~21对应毫秒，
以ASCII的十进制形式显示所以每一位减去48，得到的对应十进制数，并将其赋予对应的年月日时分秒
主函数循环中则持续输出时间
